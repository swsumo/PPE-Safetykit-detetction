# Web app
flask>=3.0.0
gunicorn>=21.0.0
werkzeug>=3.0.0

# ONNX inference (no PyTorch needed on the server)
onnxruntime>=1.16.0

# Image processing
opencv-python-headless>=4.1.1
numpy>=1.24.0

# Browser-playable (H.264) video output — cv2.VideoWriter's mp4v codec
# doesn't play inline in Chrome/Firefox, only libx264 does
imageio>=2.34.0
imageio-ffmpeg>=0.4.9

# Utils
python-dotenv>=1.0.0

# ── Local development only (not needed on Render) ──────────────────
# Training / CLI detect.py / exporting the model:
#   pip install ultralytics roboflow
# PyTorch (CPU):
#   pip install torch torchvision
# PyTorch (CUDA 12.1):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
